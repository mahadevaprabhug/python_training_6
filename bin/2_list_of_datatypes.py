"""
List of data types
"""

"""
IMMUTABLE TYPES: 
    If we create one variable with some values then we CAN'T change throught the program
    - Numbers: 
        -- We have option to keep numbers like int, float
    - Strings: 
        -- We have option to store text data like name, email-id, word, sentence etc
        -- Automatically index number will be assigned to each value
    - Tuple: 
        -- To keep multiple values like list of employee names
        -- We can keep duplicate values
        -- Automatically index number will be assigned to each value
    - Frozenset: 
        -- To keep multiple values like list of employee names
        -- We can keep unique values
        -- index number will not be there for each value

MUTABLE TYPES
    If we create one variable with some values then we CAN change throught the program

    - List: 
        -- To keep multiple values like list of employee names
        -- We can keep duplicate values
        -- Automatically index number will be assigned to each value
    - Set: 
        -- To keep multiple values like list of employee names
        -- We can keep unique values
        -- index number will not be there for each value
        
    - Dictionary: 
        -- To keep multiple values like list of employee names
        -- We can keep duplicate values
        -- We can provide our own index to each value called: KEY/VALUE pair
"""

